# just

pixi install

# it will read the pixi.toml and pixi.lock file to build the same environment for you pc. All dependencies (including PyTorch and CUDA support) are pinned in pixi.lock.
# if you interest how to do that, you could open pixi.toml read the channel line: "pixi install" will help to install the three things one by one.
